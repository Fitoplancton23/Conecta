{
  "name": "conecta-landing",
  "version": "1.0.0",
  "description": "Sitio institucional de .Conecta — gestión de redes sociales y desarrollo web. HTML semántico, SCSS con arquitectura de partials y Bootstrap.",
  "scripts": {
    "sass": "sass scss/main.scss styles/main.css --style=expanded --no-source-map",
    "dev": "sass --watch scss/main.scss styles/main.css --style=expanded --no-source-map",
    "build": "sass scss/main.scss styles/main.css --style=compressed --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.104.0"
  },
  "author": "Amalia Gross y Facundo Silveira",
  "license": "MIT"
}
